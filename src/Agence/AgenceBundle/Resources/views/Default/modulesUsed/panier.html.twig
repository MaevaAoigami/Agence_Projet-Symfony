<div class="well">
    <ul class="nav nav-list">
        <li class="nav-header">Panier</li>
        <li>
            <a href="{{ path('panier') }}">
                {% if articles == 0 %}
                    Aucune danseuse dans votre panier
                {% elseif articles == 1 %}
                    1 danseuse dans votre panier
                {% else %}
                    {{ articles }} danseuses dans votre panier
                {% endif %}
            </a>
        </li>
    </ul>
</div>
<div class="well">
    <ul class="nav nav-list">

        <li class="nav-header">Panier</li>
        
        <li>
            <a href="{{ path('panier') }}">
            {% if articles == 0 %}
            Il n'y a aucune danseuses dans votre panier
            {% elseif articles == 1 %}
            1 danseuse dans votre commande
            {% else %}
                {{ articles }} danseuses dans votre commande
            {% endif %}
            </a>
        </li>
        
    </ul>
</div>
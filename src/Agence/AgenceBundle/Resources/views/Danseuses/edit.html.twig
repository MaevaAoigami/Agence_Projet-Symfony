{% extends "::layout/layout.html.twig" %}


{% block body %}

   <div class="container">
        <div class="titre">
            <h1>Modification de profil</h1> 
        </div>

            <div class="danseuse_admin_edit">

                {{form_errors(edit_form) }}
            <form action="{{ path('admin_danseuses_update', { 'id' : entity.id }) }}" 
                  method="POST" {{ form_enctype(edit_form) }}>
                {{ form(edit_form) }}
            </form>
            
                <ul class="record_actions">
                <br>
                    <li><a class="bouton"href="{{ path('admin_danseuses') }}">Retour à la liste</a></li>
                    <br> 
                    <li>{{ form(delete_form) }}</li>

                </ul>
            </div>    
        </div>
{% endblock %}

{% extends "::layout/layout.html.twig" %}

{% block body -%}
    <h1>Danseuses edit</h1>

    <form action="{{ path('pages_danseuses_edit', { 'id' : entity.id }) }}" method="POST" {{ form_enctype(edit_form) }}>
    	{{ form(edit_form) }}
    </form>
        <ul class="record_actions">
    <li>
        <a href="{{ path('pages_danseuses') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
